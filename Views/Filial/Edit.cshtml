@model Filial;
@{
    ViewData["Title"] = "Editar Filial";
}
<h1>Editar Filial</h1>
<form asp-action="EditarFilial" asp-controller="Filial" method="post">

    <div class="row">

        <div class="col-lg-12">

            <div class="form-group">
                <label>Codigo:</label>
                <input asp-for="CodFilial" type="text" class="form-control" readonly>
            </div>

            <div class="form-group">
                <label>Nome:</label>
                <input asp-for="NomeFilial" type="text" class="form-control" required>
            </div>

            <div class="form-group">
                <div asp-validation-summary="All" class="text-danger"></div>
            </div>
            <div class="card-footer">
                <center>
                    <button type="submit" class="btn-primary">Salvar alterações</button>
                    <button type="reset" asp-action="Get" asp-controller="Filial" class="btn-cancelar">Cancelar</button>
                </center>
            </div>

        </div>

    </div>

</form>